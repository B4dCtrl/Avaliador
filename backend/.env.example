# Avaliador — variáveis de ambiente
# Copie para .env e ajuste os valores

# Porta do servidor (padrão: 8000)
PORT=8000

# Host (padrão: 0.0.0.0)
HOST=0.0.0.0

# Modo debug (True em dev, False em prod)
DEBUG=True
